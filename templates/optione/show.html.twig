{% extends 'base.html.twig' %}

{% block title %}Optione{% endblock %}

{% block body %}
    <h1>Optione</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ optione.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ optione.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('optione_index') }}">back to list</a>

    <a href="{{ path('optione_edit', {'id': optione.id}) }}">edit</a>

    {{ include('optione/_delete_form.html.twig') }}
{% endblock %}
