os = require('os')

exports.execute = (command, callback) ->

	if os.platform() is 'win32'
		commander = require('./windows')
	else
		commander = require('./unix')

	commander.executeWithPrivileges(command, callback)
